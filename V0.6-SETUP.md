# v0.6 Explorer Accounts

1. Run `supabase/v0.6-explorer-accounts.sql` in Supabase SQL Editor.
2. Initial Explorer PINs are `1234`; initial Parent PIN is `2468`.
3. Preserve your real Supabase URL/key in `config.js`. The Parent PIN no longer belongs in config.js.
4. Deploy on `feature/explorer-accounts` and test before merging.
5. Immediately change every PIN in Parent Mode.

Test: explorer check-in, wrong PIN, remembered explorer, End Adventure, Parent override, Parent dashboard, PIN changes, daily photo, secret photo, mission editors.
